// Generated by CoffeeScript 1.10.0
(function() {
  var get_random_phrase, showProperPane, store;

  store = new NoteBookStorage();

  this.deleteNotebook = function(key_name) {
    if (!confirm("Are you sure?")) {
      return;
    }
    return store.del(key_name, function(d) {
      if (d.error) {
        return alert(d.error);
      } else {
        return setTimeout('location.reload(true)', 100);
      }
    }, function(err) {
      return alert("Deleting Error" + err);
    });
  };

  showProperPane = function() {
    if (!$(".user_name").text()) {
      return $('.userTab').hide();
    } else {
      $('.userTab').tab('show');
      $('#publicPane').removeClass('active');
      return $('#userPane').addClass('active');
    }
  };

  get_random_phrase = function() {
    var i, phrases;
    phrases = ['Deep thought', 'Hello world', 'Crucial experiment', 'Great idea', 'Insight', 'Inspiration', 'Hard life', 'Genuine guess', 'Ultimate answer', 'Last attempt', 'Where is my $', 'Digital dream', 'Important', 'Meaning of life', 'Love', 'Happiness', 'Sweet dream', 'Lucky number'];
    i = Math.floor(phrases.length * Math.random());
    return phrases[i] + " ";
  };

  $(function() {
    $("#notebookList").hide();
    show_public_list('/publiclist', '#publicList');
    show_user_list('/userlist', '#userList');
    showProperPane();
    return $("#btnCreate").click(function() {
      var newName;
      newName = current_user_folder + get_random_phrase() + (100 + Math.floor(900 * Math.random()));
      newName = prompt("Change the name", newName);
      if (newName) {
        return document.location.href = "/page?owner=" + encodeURIComponent($(".user_name").text() + "|" + $(".user_network").text()) + "&access=" + encodeURIComponent("public") + "&name=" + encodeURIComponent(newName);
      }
    });
  });

}).call(this);

//# sourceMappingURL=index.html.js.map
